#lang racket
(require typed/racket)

;; Question:
;; Determine whether a given integer number is prime.
;;   Example:
;;    (is-prime 7)
;; => #t
;;

(define (is-prime x)
  (error "Not implemented yet"))
